Segmented pill control for switching periods/views. Active pill is teal with dark text.

```jsx
<TabPills tabs={['Today', 'Month', 'Year']} value={tab} onChange={setTab} />
```
Accepts plain strings or `{key,label}` objects.
