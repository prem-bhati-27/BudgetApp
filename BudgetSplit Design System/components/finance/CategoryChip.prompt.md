Category pill for pickers and filter bars. Selected = solid teal with dark text.

```jsx
<CategoryChip label="Groceries" icon="shopping-cart" color="#3ECF8E" />
<CategoryChip label="All" selected onClick={...} />
```
Use the catalog icon + color per category (see README category table).
