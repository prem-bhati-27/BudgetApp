Initials avatar for group members. White initials in a solid color circle.

```jsx
<MemberAvatar name="Aman Verma" color="#3182CE" size={44} />
<MemberAvatar name="Priya" selected />   {/* derives a stable color */}
```
Overlap a row of them for a group; `selected` adds a white ring.
