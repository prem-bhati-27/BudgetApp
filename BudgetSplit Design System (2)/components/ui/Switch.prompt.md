iOS-style toggle — the track turns teal when on, the thumb springs across.

```jsx
<Switch checked={faceId} onChange={setFaceId} />
```
Pair with a SettingsRow `right` slot for privacy & preference toggles.
